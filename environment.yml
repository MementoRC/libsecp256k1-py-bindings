name: test-conda
channels:
  - conda-forge
  - defaults
dependencies:
  - asn1crypto
  - cffi>=1.3.0
  - codecov
  - libsecp256k1 >=0.8.0,<0.9
  - pkg-config
  - pytest
  - pytest-cov
  - pytest-benchmark
  - setuptools >61
  - tox <4
  - tox-conda
